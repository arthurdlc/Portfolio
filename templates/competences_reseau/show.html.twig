{% extends 'base.html.twig' %}

{% block title %}CompetencesReseau{% endblock %}

{% block body %}
    <h1>CompetencesReseau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competences_reseau.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ competences_reseau.nom }}</td>
            </tr>
            <tr>
                <th>NiveauComp</th>
                <td>{{ competences_reseau.niveauComp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_competences_reseau_index') }}">back to list</a>

    <a href="{{ path('app_competences_reseau_edit', {'id': competences_reseau.id}) }}">edit</a>

    {{ include('competences_reseau/_delete_form.html.twig') }}
{% endblock %}
