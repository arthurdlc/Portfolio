<form method="post" action="{{ path('app_competences_s_l_a_m_delete', {'id': competences_s_l_a_m.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ competences_s_l_a_m.id) }}">
    <button class="btn">Delete</button>
</form>
