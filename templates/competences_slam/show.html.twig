{% extends 'base.html.twig' %}

{% block title %}CompetencesSLAM{% endblock %}

{% block body %}
    <h1>CompetencesSLAM</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competences_s_l_a_m.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ competences_s_l_a_m.nom }}</td>
            </tr>
            <tr>
                <th>NiveauComp</th>
                <td>{{ competences_s_l_a_m.niveauComp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_competences_s_l_a_m_index') }}">back to list</a>

    <a href="{{ path('app_competences_s_l_a_m_edit', {'id': competences_s_l_a_m.id}) }}">edit</a>

    {{ include('competences_slam/_delete_form.html.twig') }}
{% endblock %}
