{% extends 'base.html.twig' %}

{% block title %}CompetencesSLAM index{% endblock %}

{% block body %}
    <h1>CompetencesSLAM index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>NiveauComp</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for competences_s_l_a_m in competences_s_l_a_ms %}
            <tr>
                <td>{{ competences_s_l_a_m.id }}</td>
                <td>{{ competences_s_l_a_m.nom }}</td>
                <td>{{ competences_s_l_a_m.niveauComp }}</td>
                <td>
                    <a href="{{ path('app_competences_s_l_a_m_show', {'id': competences_s_l_a_m.id}) }}">show</a>
                    <a href="{{ path('app_competences_s_l_a_m_edit', {'id': competences_s_l_a_m.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_competences_s_l_a_m_new') }}">Create new</a>
{% endblock %}
